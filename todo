handling directories //

any error inside parsing request or generating the response should be thrown with error code and cleaned after that. It should not be just ignored

PATH_INFO

∗ Your program should call the CGI with the file requested as first argument.




cgi directives

session and cookies - ismail

server_name

route location

• Limit client body size.

• Setup default error pages. in config file

CGI timeout

Integri config file directives m3a lcode


Forging proper response header

Path does not exist http://127.0.0.1:8080/webserv.h

The first server for a host:port will be the default for this host:port (that means
it will answer to all the requests that don’t belong to an other server).

success exception should not close connection - keep it alive


directive cgi timeout




multiple server_names
port range
mimmetypes
url encoding
localhost->127
any -> 0
if (total_read_bytes > std::stoi(*s[0]->directive["client_max_body_size"].values[0]))